#include <cstdio>
#include <cstdlib>
#include <cstring>
using namespace std;

char num[6], den[6];

void divisions(int N, bool t[], int p){
	bool found = false;

}

int main(){
	int n;
	num[5] = den[5] = '\0';
	while(scanf("%d", &n) && n){
		memset(num, -1, 5);
		memset(den, -1, 5);
		bool taken[11] = {false};

	}
}